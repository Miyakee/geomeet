# SIT (System Integration Test) Environment Configuration
server.port=8080

# Logging Configuration
logging.level.root=INFO
logging.level.com.geomeet.api=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.file.name=logs/api-sit.log

# Database Configuration (example - adjust as needed)
# spring.datasource.url=jdbc:postgresql://sit-db-host:5432/geomeet_sit
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver

# CORS Configuration
spring.web.cors.allowed-origins=https://sit.geomeet.com
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Management Endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# Security Configuration
spring.security.require-ssl=false

