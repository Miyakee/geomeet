import { createTheme } from '@mui/material';

/**
 * Application theme configuration
 */
export const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

